import Foundation

/// Phase 2 — mock API server for adapter testing.
/// Will provide a configurable mock endpoint that adapters can hit
/// during development, returning canned responses for pull/push operations.
public actor MockServer {
    public init() {}
}
